services:
  web:
    image: traefik/whoami
    labels:
      - "arcane.domain=*.dracon.uk"
      - "arcane.resolver=dns-resolver"
    ports:
      - "8080:80"
    restart: always

# Auto-Ingress should:
# 1. Remove port 8080 mapping
# 2. Add Host(ingress-demo.dracon.uk)
# 3. Add network traefik-public
